# Dot Mole Whack 2.0 재설계 명세

## 1. 재설계 목적

현재 버전은 5×4, 총 20칸의 보드에서 커서를 한 칸씩 움직여 두더지를 찾고 잡는 구조다. 두더지는 난이도에 따라 약 1.0~1.8초 동안만 유지되므로, 화면을 보지 않고 음성 또는 닷패드 촉각으로 위치를 파악한 뒤 커서를 이동해야 하는 플레이어에게는 실제 반응 게임이 아니라 운에 가까운 탐색 게임이 된다.

2.0 버전은 다음 문제를 해결한다.

- 두더지 위치를 찾기 위해 20칸을 탐색해야 하는 부담 제거
- 음성 안내가 끝나기 전에 두더지가 사라지는 불공정한 타이밍 제거
- 닷패드에서 보드와 커서와 두더지를 동시에 구분해야 하는 인지 부담 제거
- 실수 몇 번으로 게임이 즉시 종료되어 학습 기회가 사라지는 구조 제거
- 시각장애인이 도움 없이 시작, 플레이, 일시 정지, 결과 확인, 재도전할 수 있도록 개선

## 2. 핵심 콘셉트

### 이동하지 말고 바로 잡기

두더지는 다음 다섯 위치 중 하나에만 등장한다.

1. 왼쪽
2. 오른쪽
3. 위쪽
4. 아래쪽
5. 가운데

플레이어는 커서를 이동하지 않는다. 신호를 듣고, 보고, 촉각으로 확인한 뒤 해당 입력을 한 번 누른다.

이 구조에서 게임의 재미는 “칸을 찾는 속도”가 아니라 “방향 신호를 이해하고 정확하게 반응하는 속도”에서 나온다.

## 3. 기본 조작

| 방향 | 키보드 | DotPad | 화면/터치 |
| --- | --- | --- | --- |
| 왼쪽 | `←` | `F1` 또는 왼쪽 패닝키 | 왼쪽 구멍 누르기 |
| 오른쪽 | `→` | `F2` 또는 오른쪽 패닝키 | 오른쪽 구멍 누르기 |
| 위쪽 | `↑` | `F3` 또는 위 패닝키 | 위쪽 구멍 누르기 |
| 아래쪽 | `↓` | `F4` 또는 아래 패닝키 | 아래쪽 구멍 누르기 |
| 가운데 | `Space` 또는 `Enter` | 전체 패닝키 | 가운데 구멍 누르기 |

보조 조작:

- `P`: 일시 정지 또는 계속
- `R`: 현재 두더지 신호 다시 듣기
- `S`: 현재 라운드, 점수, 명중률 듣기
- `H`: 도움말
- `Esc`: 로비로 이동

## 4. 게임 흐름

### 4.1 인트로 화면

인트로 화면에서는 게임의 차별점을 한 문장으로 명확하게 설명한다.

> 20칸을 헤매지 않아도 됩니다. 두더지 방향을 듣고·보고·촉각으로 느낀 뒤, 해당 방향키를 한 번만 누르세요.

필수 설정:

- 반응 시간
  - 여유롭게: 4초
  - 기본: 3초
  - 빠르게: 2.2초
- 라운드 수
  - 10라운드
  - 15라운드
  - 20라운드

주요 버튼:

- 게임 시작
- 시간 제한 없이 연습

닷패드 조작표는 시작 버튼 바로 위 또는 아래에 항상 노출한다.

### 4.2 연습 모드

연습 모드는 5라운드이며 시간 제한이 없다.

- 다섯 방향을 무작위로 안내한다.
- 신호 반복 횟수에 제한을 두지 않는다.
- 잘못 누르면 정답 방향을 다시 안내한다.
- 실패로 인해 연습이 종료되지 않는다.
- 완료 후 명중 수만 간단하게 알려준다.

연습 모드는 처음 접속한 사용자에게 가장 먼저 추천하되 강제하지 않는다.

### 4.3 일반 게임

한 라운드의 순서는 다음과 같다.

1. 이전 피드백이 종료된다.
2. 화면과 닷패드에 새로운 방향이 동시에 표시된다.
3. 짧은 방향 효과음이 재생된다.
4. TTS가 “두더지 왼쪽”과 같이 말한다.
5. 플레이어가 직접 방향을 선택한다.
6. 명중, 오답, 시간 초과 결과를 화면·음성·촉각으로 동시에 제공한다.
7. 약 1초 후 다음 라운드로 이동한다.

게임은 생명제가 아니라 고정 라운드제로 진행한다. 실수해도 다음 라운드에서 다시 참여할 수 있다.

## 5. 상태 구조

권장 상태 머신:

```text
LOBBY
  → COUNTDOWN
  → CUE
  → WAITING_FOR_INPUT
  → FEEDBACK_HIT | FEEDBACK_WRONG | FEEDBACK_TIMEOUT
  → NEXT_ROUND
  → RESULT
```

추가 상태:

```text
PAUSED
HELP_DIALOG
DEVICE_CONNECTING
DEVICE_DISCONNECTED
```

상태 전환 원칙:

- 도움말을 열면 자동 일시 정지
- 도움말을 닫으면 같은 신호를 처음부터 다시 제시
- 브라우저 탭이 숨겨지면 자동 일시 정지
- 재개 시 라운드를 건너뛰거나 오답 처리하지 않음
- TTS가 겹치지 않도록 새 안내 전 이전 음성을 중지

## 6. 점수 체계

반응 시간이 빠른 사용자만 과도하게 유리해지지 않도록, 음성 안내 사용 여부와 관계없이 이해하기 쉬운 고정 점수를 사용한다.

- 기본 명중: 100점
- 연속 명중 보너스: 2연속부터 회당 20점 증가
- 연속 보너스 상한: 100점
- 오답 또는 시간 초과: 점수 차감 없음, 연속 명중만 초기화

예시:

- 첫 명중: 100점
- 두 번째 연속 명중: 120점
- 세 번째 연속 명중: 140점
- 여섯 번째 이상 연속 명중: 최대 200점

결과 화면 표시 항목:

- 최종 점수
- 명중 횟수 / 전체 라운드
- 명중률
- 최고 연속 명중

## 7. 음성 및 효과음 설계

### 7.1 TTS

필수 안내:

- 게임 시작과 총 라운드 수
- 현재 방향
- 명중 여부
- 오답일 때 정답 방향
- 시간 초과일 때 정답 방향
- 일시 정지와 재개
- 현재 점수와 명중률
- 최종 결과

방향 문구는 짧고 일정하게 유지한다.

- “두더지 왼쪽”
- “두더지 오른쪽”
- “두더지 위쪽”
- “두더지 아래쪽”
- “두더지 가운데”

장황한 문장을 매 라운드 반복하지 않는다.

### 7.2 방향 효과음

효과음은 보조 정보이며 단독 정보가 되어서는 안 된다.

- 왼쪽: 낮은 음, 좌측 스테레오
- 오른쪽: 중간 음, 우측 스테레오
- 위쪽: 가장 높은 음, 중앙
- 아래쪽: 낮고 짧은 음, 중앙
- 가운데: 둥근 단일음 또는 두 번의 짧은 음

명중과 실패 효과음도 서로 명확하게 구분한다.

- 명중: 위로 올라가는 2음
- 오답 또는 시간 초과: 아래로 내려가는 2음

`prefers-reduced-motion`과 별개로 효과음 끄기 기능을 제공한다.

## 8. DotPad 촉각 설계

### 8.1 기본 원칙

기존처럼 5×4 보드 전체를 축소해 표시하지 않는다. 매 라운드마다 DotPad 전체 영역을 활용해 하나의 큰 방향 기호만 표시한다.

- 왼쪽: 중앙에서 왼쪽 끝으로 향하는 굵은 화살표
- 오른쪽: 중앙에서 오른쪽 끝으로 향하는 굵은 화살표
- 위쪽: 중앙에서 위쪽 끝으로 향하는 굵은 화살표
- 아래쪽: 중앙에서 아래쪽 끝으로 향하는 굵은 화살표
- 가운데: 중앙의 큰 원 또는 이중 원

### 8.2 피드백 패턴

- 명중: 큰 체크 패턴
- 오답: 큰 X 패턴
- 시간 초과: X 패턴 후 정답 방향을 다시 1초 표시
- 대기: 다섯 위치를 나타내는 작은 기준점만 표시
- 일시 정지: 모든 핀을 내리거나 중앙에 가로선 표시

### 8.3 입력 매핑

- F1: 왼쪽
- F2: 오른쪽
- F3: 위쪽
- F4: 아래쪽
- 전체 패닝키: 가운데

기기에서 위·아래 패닝키 코드가 제공되는 경우 보조 입력으로 연결한다. 기능키만으로도 모든 방향을 선택할 수 있어야 한다.

## 9. 화면 UI 구조

### 9.1 플레이 화면

상단:

- 현재 상태 문장
- 일시 정지 버튼

HUD:

- 현재 라운드 / 전체 라운드
- 점수
- 연속 명중
- 명중률

중앙 무대:

```text
        [ 위쪽 ]
[ 왼쪽 ][ 가운데 ][ 오른쪽 ]
        [ 아래쪽 ]
```

각 위치는 최소 44×44px 이상의 버튼으로 구현하며, 모바일에서는 손가락으로 정확히 누를 수 있도록 가능한 한 크게 유지한다.

하단:

- 신호 다시 듣기
- 현황 듣기
- 음성 켜기/끄기
- 닷패드 연결
- 도움말

### 9.2 시각 스타일

- 이미지 의존도를 낮추고 CSS로 밝고 유쾌한 정원 무대 표현
- 진한 청록 배경 + 초록 잔디 + 주황 두더지 조합
- 활성 두더지는 테두리, 형태, 텍스트가 동시에 변함
- 색상만으로 상태를 구분하지 않음
- 과도한 번쩍임, 화면 전체 플래시, 빠른 흔들림 금지
- 반투명 패널은 텍스트 대비 4.5:1 이상 확보

### 9.3 반응형

- 데스크톱: 중앙 무대 최대 폭 약 570px
- 모바일: 다섯 버튼이 화면 안에서 잘리지 않도록 간격 축소
- 짧은 높이의 임베드: 상단 여백과 HUD 높이 축소
- `?embed=1`: 배경 투명, 그림자 제거, 스크롤 없이 핵심 플레이 영역 노출

## 10. 접근성 요구사항

### 10.1 키보드

- 모든 기능 키보드 접근 가능
- 방향키 입력 시 페이지 스크롤 방지
- 모달 닫힘 후 호출 버튼 또는 게임 영역으로 포커스 복귀
- 포커스 표시 3px 이상, 배경과 고대비

### 10.2 스크린리더

- 현재 상태: `role="status"`, `aria-live="polite"`
- 명중, 오답, 종료처럼 즉시 알려야 하는 결과: 별도 `role="alert"`
- 방향 버튼: “왼쪽 선택”, “오른쪽 선택”처럼 명확한 이름 사용
- 시각 장식용 두더지와 잔디: `aria-hidden="true"`
- HUD는 항목명과 값을 함께 읽을 수 있도록 구성

### 10.3 모션

`prefers-reduced-motion: reduce`에서는 다음을 제거한다.

- 두더지가 빠르게 튀어나오는 애니메이션
- 게이지 애니메이션
- 버튼 이동 효과
- 결과 화면 확대 효과

상태 변화는 정적인 테두리와 텍스트로 계속 구분되어야 한다.

### 10.4 독립 플레이 완료 조건

눈을 가린 상태에서 다음 과정을 도움 없이 수행할 수 있어야 한다.

1. 게임 목적 이해
2. 연습 모드 시작
3. 다섯 방향 입력
4. 현재 신호 반복
5. 현재 점수 듣기
6. 일시 정지 및 재개
7. 게임 종료 결과 확인
8. 다시 하기 또는 로비 이동

## 11. Tactile Worlds 연동

기존 메시지 구조를 유지하되 세부 정보를 확장한다.

```js
postMessage({
  source: 'dot-mole-whack',
  type: 'game-ready',
  version: '2.0.0',
  controlModel: 'direct-response'
})
```

```js
postMessage({
  source: 'dot-mole-whack',
  type: 'game-start',
  mode: 'game' | 'practice',
  difficulty: 'guided' | 'normal' | 'fast',
  rounds: 10
})
```

```js
postMessage({
  source: 'dot-mole-whack',
  type: 'game-complete',
  mode: 'game' | 'practice',
  score: 1200,
  hits: 9,
  rounds: 10,
  accuracy: 90,
  bestStreak: 5
})
```

## 12. 구현 우선순위

### P0: 핵심 플레이 구조

- 5×4 보드 및 이동 커서 제거
- 다섯 방향 직접 입력
- 고정 라운드제
- 연습 모드
- 신호 반복과 현황 듣기
- 일시 정지와 재개

### P1: 멀티모달 피드백

- 방향별 TTS
- 방향별 효과음
- 닷패드 대형 화살표
- 명중 체크와 실패 X 패턴

### P2: 완성도

- 결과 화면
- 임베드 최적화
- 첫 방문 연습 추천
- 로컬 최고 기록
- 세부 애니메이션과 시각 효과

## 13. QA 체크리스트

### 기능

- 다섯 방향이 고르게 등장하는가
- 직전 방향이 과도하게 연속 등장하지 않는가
- 한 라운드에서 입력이 두 번 처리되지 않는가
- 오답 후 점수가 차감되지 않고 연속 기록만 초기화되는가
- 마지막 라운드 결과가 누락되지 않는가
- 연습 모드에서 시간 초과가 발생하지 않는가

### 일시 정지

- 활성 신호 중 일시 정지해도 해당 라운드가 실패 처리되지 않는가
- 재개 시 같은 신호가 다시 안내되는가
- 도움말을 열었다 닫아도 라운드를 건너뛰지 않는가
- 탭을 전환했다 돌아와도 자동으로 게임이 진행되지 않는가

### 접근성

- 화면을 보지 않고 키보드만으로 전체 플레이가 가능한가
- TTS가 중복 재생되지 않는가
- 음성 끄기 후 ARIA 상태 안내는 유지되는가
- 200% 확대에서 버튼과 텍스트가 잘리지 않는가
- 390×844, 430×700 모바일에서 스크롤 없이 핵심 무대가 보이는가
- 1366×768 임베드에서 상하 스크롤이 발생하지 않는가

### DotPad

- 연결 성공과 해제가 음성으로 안내되는가
- 화살표 방향을 손끝으로 명확히 구분할 수 있는가
- 기능키 입력이 한 번만 처리되는가
- 기기 연결 실패 후 웹 플레이가 정상 유지되는가
- 결과 화면 진입 시 이전 방향 패턴이 남지 않는가

## 14. 완료 기준

2.0 재설계는 다음 조건을 모두 충족할 때 완료로 본다.

- 시각장애인 테스터가 5분 이내에 조작을 이해한다.
- 도움 없이 연습 모드와 일반 게임을 각각 완료한다.
- 잘못 누른 이유와 정답 방향을 스스로 이해한다.
- 닷패드에서 다섯 방향 촉각 패턴을 90% 이상 구분한다.
- 데스크톱, 모바일, 임베드에서 레이아웃 클리핑이 없다.
- 콘솔 오류와 처리되지 않은 Promise 오류가 없다.
- `game-ready`, `game-start`, `game-complete` 메시지가 정상 전송된다.
